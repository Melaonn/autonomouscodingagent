# Local loopback access falls back to a setup session only when OAuth is unavailable.
PORT=4310
HOST=127.0.0.1
PUBLIC_URL=http://localhost:4310
# Production uses PostgreSQL; absent DATABASE_URL uses persistent local PGlite.
DATABASE_URL=
DATA_DIR=.runtime
SESSION_SECRET=replace-with-at-least-32-random-characters
# Optional: only for the legacy local connect-codex.ps1 development path.
LOCAL_MCP_TOKEN=
# Installation-wide identity provider configuration, owned by the application operator.
# End users never set these values. OAuth callback URL:
# http://localhost:4310/auth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# Comma-separated GitHub usernames. Production requires both allowlists.
GITHUB_ALLOWED_USERS=
GITHUB_ADMIN_USERS=
# Optional production alternative: a GitHub App installation may own repository automation.
GITHUB_APP_ID=
GITHUB_INSTALLATION_ID=
GITHUB_APP_PRIVATE_KEY_FILE=
