{
  "name": "@sdlc/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=../../.env src/index.ts",
    "build": "esbuild src/index.ts src/chat-main.ts src/eval-main.ts src/paired-run-main.ts src/benchmark-check-main.ts --bundle --platform=node --format=esm --packages=external --outdir=dist"
  }
}
