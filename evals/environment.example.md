# Benchmark environment

Work autonomously in this checkout. Do not commit, push, publish, or deploy. Use only task-visible repository content and the public verification command supplied by the benchmark operator. Hidden checks are evaluated after both variants finish.
